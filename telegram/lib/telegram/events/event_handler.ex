defmodule TelegramService.Events.EventHandler do
  @moduledoc "Respond to incoming blockchain events"

  def handle_event(event) do
    # start a new process to handle event
    # get subscribers for event
    # if subscribers
        # format a message
        # start a new process for each event send
  end
end